/**
 * @deprecated Prefer `CookieBanner` (Consent Mode v2 + chave `cookie_consent`).
 * Mantido como alias para não quebrar imports antigos.
 */
export { CookieBanner as CookieConsent, COOKIE_CONSENT_KEY } from './CookieBanner';
